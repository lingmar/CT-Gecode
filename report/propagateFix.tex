\IF{\CurrTable~has changed since last invocation}       
  \IF{($index \leftarrow \CurrTable$.indexOfFixed())$ \ \neq -1$}
  %\STATE \FixDomains($index$)
    \RETURN $\Tuple{\SUBSUMED, s[x \mapsto T[index][x] : x \in \Vars]}$
  \ELSE
    \STATE $s \leftarrow \FilterDomains(s)$
  \ENDIF
\ENDIF