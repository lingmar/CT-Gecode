\IF[executed in the propagator function]{some varible was pruned since last time}
  \IF{$index \leftarrow \CurrTable$.fixed()$\neq -1$}
    \STATE \FixDomains($index$)
    \RETURN{\SUBSUMED}
  \ELSE
    \STATE \FilterDomains()
  \ENDIF
\ENDIF
  